import React, { useEffect, useRef, useState } from "react";
import ReactDOM from "react-dom";
import Tab from "./Tab";
import styles from "./Session.scss";

export default function Session(props: WindowProps) {
  return <div></div>;
}
